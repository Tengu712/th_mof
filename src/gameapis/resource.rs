/// A enum for key of image resource hash map.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum ImgResID {
    SpeechBubble,
    StageTitleBamboo,
    Udonge,
    Tei,
    Black,
    Red,
    StageBamboo,
}
